const search=document.querySelector('#search');const source=document.querySelector('#source');const rows=[...document.querySelectorAll('#paper-body tr')];const count=document.querySelector('#visible-count');function filter(){const q=(search?.value||'').trim().toLowerCase();const s=source?.value||'';let visible=0;rows.forEach(row=>{const show=(!q||row.dataset.search.includes(q))&&(!s||row.dataset.source===s);row.hidden=!show;if(show)visible++});if(count)count.textContent=visible}search?.addEventListener('input',filter);source?.addEventListener('change',filter);